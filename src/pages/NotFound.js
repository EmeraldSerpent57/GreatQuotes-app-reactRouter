const NotFound = () => {
    return (
        <div className="centered">
            <p>Page Not Found!</p>
        </div>
    );
};

export default NotFound;

//Fallback page to show if the URL is not valid